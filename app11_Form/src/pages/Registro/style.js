import { StyleSheet, Dimensions } from "react-native";

const styleUsuario = StyleSheet.create({
  boxUsuario: {
    marginTop: 15,
    marginLeft: 15,
    marginRight: 15,
    backgroundColor: '#fff',
    marginBottom: 10,
    padding: 15,
    borderColor: '#cecece',
    borderWidth: 2,
    borderRadius: 3,
    borderStyle: 'dashed'
  },
  title: {
    marginBottom: 5,
    textAlign: "center",
    fontSize: 22,
    fontWeight: 'bold',
    color: '#000'
  },
  txt: { fontSize: 16, color: '#000' }
})

export default styleUsuario